<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.nisonnaeson">

    <!-- 권한 추가 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.Nisonnaeson"
        tools:targetApi="31">

        <!-- FileProvider 설정 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name="com.example.nidonnaedon.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.nidonnaedon.AccountViewActivity"
            android:exported="true" />
        <activity
            android:name="com.example.nidonnaedon.ReportActivity"
            android:exported="false" />
        <activity
            android:name="com.example.nidonnaedon.InputViewActivity"
            android:windowSoftInputMode="adjustPan"
            android:exported="true"/>
        <activity
            android:name="com.example.nidonnaedon.LoginActivity"
            android:exported="true" />
        <activity
            android:name="com.example.nidonnaedon.MainView"
            android:exported="true" />
        <activity
            android:name="com.example.nidonnaedon.MainActivity_page8"
            android:windowSoftInputMode="adjustPan"
            android:exported="true"/>
        <activity
            android:name="com.example.nidonnaedon.MyPageView"
            android:exported="true" />
        <activity
            android:name="com.example.nidonnaedon.MainActivity_page7"
            android:exported="true" />
    </application>

</manifest>
